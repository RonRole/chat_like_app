version: 2
jobs:
  build:
    machine: true
    steps:
      - checkout
      - run:
          name: Build Docker Image
          command: docker-compose build
      - run:
          name: docker-compose up
          command: |
                docker-compose up
      # - run:
      #     name: Run tests
      #     command:
      - run:
          name: docker-compose down
          command: |
                    docker-compose down