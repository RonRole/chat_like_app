build:
  docker:
    web: ./Heroku.Dockerfile
    worker: ./Heroku.Dockerfile

run:
  web: |
    cd /usr/src/front
    serve -s build
  worker: |
    bundle exec rake db:migrate
    bundle exec puma -C config/puma.rb -b "localhost" -p 4000 
