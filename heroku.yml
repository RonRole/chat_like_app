build:
  docker:
    web: ./Heroku.Dockerfile

run:
  web: |
    bundle exec rake db:migrate
    echo "bundle exec puma -C config/puma.rb -p 4000" | sh
    cd /usr/src/front
    sh -c "serve -s build"