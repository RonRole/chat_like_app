build:
  docker:
    web: ./DockerfileForHeroku

run:
  web: |
    bundle exec rake db:migrate
    bundle exec puma -C config/puma.rb -p 4000 -d
    cd /usr/src/front
    echo "REACT_APP_BACKEND_ADDRESS=${REACT_APP_BACKEND_ADDRESS}" > .env
    serve -s build