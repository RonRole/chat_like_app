build:
  docker:
    web: ./Heroku.Dockerfile

release:
  command:
    - cd /usr/src/front & yarn build
  image: docker
run:
  web: |    
    cd /usr/src/front
    node ./heroku-express.js & \

    cd /usr/src/app 
    bundle exec rake db:migrate 
    bundle exec puma -C config/puma.rb -p $API_PORT　-e production & \
        
    cd /usr/src/socket
    node ./socket_server.js  