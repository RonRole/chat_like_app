build:
  docker:
    web: ./Heroku.Dockerfile
run:
  web: |    
    cd /usr/src/front
    node ./heroku-express.js & \

    cd /usr/src/app
    bundle exec rake secret | xargs export $SECRET_KEY_BASE={}
    bundle exec rake db:migrate 
    bundle exec puma -C config/puma.rb -p $API_PORT