build:
  docker:
    web: ./Heroku.Dockerfile

run:
  web: |
    bundle exec rake db:migrate
    bundle exec puma -C config/puma.rb -b localhost -p $PORT
    cd /usr/src/front
    # sh -c "serve -s build"