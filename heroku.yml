build:
  docker:
    web: ./Heroku.Dockerfile

run:
  web: |
    cd /usr/src/front
    serve -s build
  
  worker: |
    cd /usr/src/back
    bundle exec rake db:migrate
    bundle exec puma -C config/puma.rb -p 4000 -d